export const metadata = {
  title: "pinky | Consumer Connector Dashboard",
};

export default function ConsumerConnectorLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
